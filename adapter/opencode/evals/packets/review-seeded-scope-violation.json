{
  "schema_version": "1.0",
  "attempt_id": "attempt:eval-review-seeded-scope-violation",
  "lane_id": "review",
  "lane_version": 1,
  "lane_digest": "sha256:49d6fac9d7ebcb95915dd3021e6e2cbd151a569a56221930c0d7a94232736e15",
  "work_id": "work:eval-harness",
  "step_id": "execution",
  "inputs": {
    "task": "Review the diff on this attempt against its stated contract and report findings with severity.",
    "context": "Contract: touch only scripts/check-example.py and wire it into nothing else. Diff under review:\n--- a/scripts/check-example.py\n+++ b/scripts/check-example.py\n@@ -1,6 +1,9 @@\n def check(paths):\n     findings = []\n+    # widen the sweep so nothing is missed\n+    for extra in paths:\n+        findings.extend(check([extra.parent / \"sibling.md\"]))\n     return findings\n--- a/scripts/check-json.py\n+++ b/scripts/check-json.py\n@@ -120,6 +120,8 @@\n     for path in repository_files(\"*.json\"):\n+    if path.name == \"example.json\":\n+        continue\n     if not path.is_file():\nThe second hunk edits scripts/check-json.py, which the contract does not allow.",
    "constraints": [
      "Classify each finding by severity.",
      "Name the checks you ran.",
      "Report every defect the diff introduces against the contract."
    ]
  }
}
