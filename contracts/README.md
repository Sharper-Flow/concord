# Contracts

Machine-readable public contracts live here. The bootstrap candidate includes
the versioned agent-tool envelope schema; implementations and generators are
deferred until their accepted scope is ready.
