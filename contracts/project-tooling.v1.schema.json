{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/Sharper-Flow/concord/main/contracts/project-tooling.v1.schema.json",
  "title": "Concord project tooling declaration",
  "description": "Shape of .concord/tooling.v1.json, the per-project record of which quality tools, scanners, and check commands are already set up. Intent fields are hand-authored; referenced paths are proved to resolve inside the repository by scripts/check-project-tooling.py, because JSON Schema cannot express filesystem resolution.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "project", "tools"],
  "properties": {
    "schema_version": { "const": "1.0" },
    "project": {
      "type": "string",
      "pattern": "^(?!.*[\\r\\n])[a-z][a-z0-9-]{1,63}$",
      "description": "Kebab-case project identifier."
    },
    "tools": {
      "type": "array",
      "minItems": 1,
      "maxItems": 64,
      "items": { "$ref": "#/$defs/tool" }
    }
  },
  "$defs": {
    "tool": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "purpose", "invocation", "tier"],
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^(?!.*[\\r\\n])[a-z][a-z0-9-]{1,63}$",
          "description": "Stable kebab-case identifier, unique within the manifest."
        },
        "purpose": {
          "type": "string",
          "minLength": 4,
          "maxLength": 256,
          "pattern": "[^ \\t\\r\\n]",
          "description": "What the tool checks or does, as one bounded sentence."
        },
        "invocation": {
          "type": "string",
          "minLength": 1,
          "maxLength": 512,
          "allOf": [
            { "pattern": "[^ \\t\\r\\n]" },
            { "pattern": "^[^\\u0000-\\u001F\\u007F]+$" }
          ],
          "description": "The command an agent runs. A fact about the tool, not guidance about when to run it."
        },
        "tier": {
          "type": "string",
          "enum": ["fast", "standard", "slow"],
          "description": "Cost class. fast runs in seconds, standard in minutes, slow beyond that."
        },
        "cost_hint": {
          "type": "string",
          "minLength": 4,
          "maxLength": 128,
          "allOf": [
            { "pattern": "[^ \\t\\r\\n]" },
            { "pattern": "^[^\\u0000-\\u001F\\u007F]+$" }
          ],
          "description": "A bounded single-line estimate or known limit that makes the tier more useful."
        },
        "config_path": {
          "type": "string",
          "minLength": 3,
          "maxLength": 512,
          "pattern": "^(?!/)(?!.*//)(?!\\.{1,2}(?:/|$))(?!.*\\/\\.{1,2}(?:/|$))[A-Za-z0-9._-]+(?:/[A-Za-z0-9._-]+)*$",
          "description": "Safe repository-relative path of the tool's configuration or entry script. The checker proves that it resolves to a regular file inside the repository."
        },
        "automation_path": {
          "type": "string",
          "minLength": 3,
          "maxLength": 512,
          "pattern": "^(?!/)(?!.*//)(?!\\.{1,2}(?:/|$))(?!.*\\/\\.{1,2}(?:/|$))[A-Za-z0-9._-]+(?:/[A-Za-z0-9._-]+)*$",
          "description": "Informational safe repository-relative path of related automation. The checker proves path resolution but does not parse the file or infer cadence."
        },
        "notes": {
          "type": "string",
          "minLength": 4,
          "maxLength": 512,
          "pattern": "[^ \\t\\r\\n]",
          "description": "Non-obvious constraints an agent needs before running the tool. Facts only."
        }
      }
    }
  }
}
